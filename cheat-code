1) How to remove duplicate from array ---

const arr = [2,4,5,6,6,7,7,7,8]

const newArr = [...new Set(arr)];

2) How to sort array

newArr.sort((a,b) => a -b);



IF YOU WANT TO ADD TWO OBJCET VALUE IN JS WE CAN DO IT BY USING SPREAD OPERATOR:::


for Example:

const input1 = { a:1, b:2, c:3, d:10, e:12};

let objtoLoop = {test: 2};

objectLoop = {...objtoLoop, ...input1} -----> without spread opertor complete object will add on input1;



3) WHILE USING REDUCE IT WILL ITERATE OVER ALL ELEMENT, MAP WILL RETURN  SAME ORIGINAL LENGTH OF ARRAY, FILTER CAN"T UPDATE ARRAY VALUES
